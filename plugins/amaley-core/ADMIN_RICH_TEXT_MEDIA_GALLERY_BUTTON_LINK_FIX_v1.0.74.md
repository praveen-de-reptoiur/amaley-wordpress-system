# Amaley Core v1.0.74 — Admin Rich Text, Media Gallery and Related Button Link Fix

## Scope

Safe maintenance update after Cluster Archive and SHG Single visual work.

## Fixed

- Enqueued WordPress editor scripts on Amaley Cluster, SHG Group and Member / Producer edit screens so rich text fields remain reliable inside metaboxes.
- Added a submit sync for TinyMCE editors so Full Story content saves properly from Visual/Code mode.
- Converted Cluster, SHG Group and Member / Producer gallery fields from manual URL-only workflow into Media Library upload/select controls.
- Gallery field still stores clean image URLs internally, so existing frontend gallery sections remain compatible.
- Cluster Single linked SHG / producer cards now render real links instead of muted non-clickable buttons.

## Not touched

- No Cluster Archive layout/design changes.
- No SHG Single layout/design changes.
- No card design changes.
- No WooCommerce logic changes.
- No header/footer/theme changes.
- No frontend CSS redesign.
